{
  "name": "didilock",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite --host",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d dist"
  },
  "devDependencies": {
    "typescript": "^5.2.2",
    "vite": "^5.0.8"
  },
  "dependencies": {
    "@rive-app/canvas": "^2.23.8",
    "gh-pages": "^6.2.0"
  },
  "homepage": "https://elbert-git.github.io/DidILock/"
}
